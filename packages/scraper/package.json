{
  "name": "@swarm/scraper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.777.0",
    "@swarm/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @swarm/scraper'",
    "test": "echo 'No tests configured for @swarm/scraper'"
  }
}
