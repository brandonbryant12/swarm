{
  "name": "@swarm/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.16",
    "@orpc/server": "^1.9.0",
    "ai": "^5.0.76",
    "effect": "^3.17.14",
    "pg": "^8.18.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @swarm/backend'",
    "test": "echo 'No tests configured for @swarm/backend'"
  }
}
