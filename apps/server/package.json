{
  "name": "@swarm/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./contracts": {
      "types": "./src/contracts.ts",
      "default": "./src/contracts.ts"
    }
  },
  "dependencies": {
    "@hono/node-server": "^1.19.4",
    "@orpc/server": "^1.9.0",
    "@swarm/backend": "workspace:*",
    "better-auth": "^1.3.12",
    "dotenv": "^16.4.7",
    "hono": "^4.8.3",
    "pg": "^8.18.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @swarm/server'",
    "test": "echo 'No tests configured for @swarm/server'"
  }
}
