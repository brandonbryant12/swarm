{
  "name": "@swarm/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "swarm": "./src/index.ts"
  },
  "dependencies": {
    "@swarm/db": "workspace:*",
    "@swarm/scraper": "workspace:*",
    "commander": "^13.1.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @swarm/cli'",
    "test": "echo 'No tests configured for @swarm/cli'"
  }
}
